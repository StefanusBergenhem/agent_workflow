# docs/MEMORY.yaml — Project learning memory
# Managed by wf-skill-continuous-learning after each sprint.
# Consumed by build, review, and SWA skills as context.
# Human-reviewable: inspect lessons after each sprint before committing.
#
# Categories:
#   contract_patterns    - Task contract quality rules (context_to_load, files_to_touch)
#   component_rules      - Component-specific rules (always load file X for component Y)
#   rejection_patterns   - Recurring rejection types and proven fixes
#   process_rules        - Pipeline/workflow improvements
#   architecture_signals - Design-level patterns for SA/SWA

version: 1
max_entries: 30

lessons: []
