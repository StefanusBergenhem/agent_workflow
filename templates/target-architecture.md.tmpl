# Target Architecture

> Last updated: YYYY-MM-DD
> Status: Draft

## Product Vision

<!-- High-level product vision: tagline, target users, value proposition.
     This section aligns with roadmap.yaml but focuses on the architectural implications. -->

## Architecture Overview

<!-- System context: major components, external integrations, deployment model.
     Include tech stack with rationale for key choices.
     Diagrams here are persistent (unlike ephemeral SA session diagrams) —
     this is the one place where architecture diagrams live in a file. -->

## Key Decisions

<!-- Numbered decisions that define the target state.
     Each decision should include: context, alternatives considered, rationale.
     Reference ADRs where they exist (e.g., "See ADR-003 for full analysis").

     Decisions here are "agreed but not yet implemented" — once implemented,
     they should have a corresponding ADR in docs/adrs/.

     Format:
     ### Decision N: Title
     - **Context:** Why this decision needs to be made
     - **Decision:** What was decided
     - **Alternatives rejected:** What else was considered and why not
     - **Rationale:** Why this choice -->

## Data Architecture

<!-- Entity models, schemas, relationships, storage choices.
     Focus on the TARGET state, not current state. -->

## Modularity & Configurability

<!-- What is data-driven vs hard-coded. Runtime configuration strategy.
     Extension points and plugin boundaries. -->

## Migration Path

<!-- What stays from the current architecture, what changes, what is new.
     This section bridges COMPONENTS.yaml (current) and this document (target).

     | Component | Change | Effort |
     |-----------|--------|--------|
     | ...       | ...    | ...    | -->
